using System;

public class NumberSetCalculator
{
	public NumberSetCalculator()
	{    //метод возвращения суммы
		public int GetSum()
		{   
			int value1;
			int value2;
			int result = value1 + value2;
			if 
			return result;
			Console.WriteLine("результат суммы ${result}");
		}
		//метод генерации массива
		public int GenerateArray()
		{
			int[] numbers[6] = { };
			return numbers[6];

		}
        //метод сортировки
		public int sortArray()
		{
            int[] numbers[6] = { };
            return numbers[6];
			for(int i=0; i < numbers.length(); i++)
			{
				numbers[i] = 0;
				i++;
				return numbers[i];
				Console.WriteLine("результат сортировки ${numbers[i]}")
			}
        }

        //метод возвращения резульата умножения
    public int getMulnition() {
			int value1;
			int value2;
			int result = value1 + value2;
			return result;
			Console.WriteLine("результат суммы ${result}");
		}
    }
}
